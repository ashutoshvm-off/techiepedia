export interface Dimensions {
  width: number;
  height: number;
}

export interface ResponsiveSpacing {
  padding: {
    vertical: string;
    horizontal: string;
  };
  margin: {
    vertical: string;
    horizontal: string;
  };
}
